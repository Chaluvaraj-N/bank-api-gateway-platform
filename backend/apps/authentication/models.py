from __future__ import annotations

"""Authentication module models.

This phase relies on the existing `backend.apps.users.models.User` table as the
source of truth for identity and credentials.

No additional DB tables are introduced yet.
"""


